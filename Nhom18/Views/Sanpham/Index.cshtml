@model IEnumerable<Nhom18.Models.Sanpham>

@{
    ViewData["Title"] = "Index";
}
<h1 style="text-align:center;" class="w3-animate-right"> Danh sách Sản phẩm </h1>

<p>
    <a asp-action="Create"  class="w3-btn w3-round-xxlarge w3-button w3-purple">Thêm Sản phẩm mới</a>
</p>
<form asp-controller="SanPham" asp-action="Index">
    <p >
        <p>
    <a asp-action="Upload" class="w3-btn w3-round-xxlarge w3-button w3-purple" >Upload ⬆</a>
</p>
        Tiêu đề: <input type="text" name="SearchString" class="w3-button w3-white w3-border w3-border-red w3-round-large" />
        <input type="submit" value="🔎" class="w3-button w3-white w3-border w3-border-red w3-round-large" />
    </p>
</form>
<table class="w3-table-all w3-card-4">
    <thead>
        <tr  class="w3-hover-black">
            <th  >
                @Html.DisplayNameFor(model => model.MaSP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenSP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GiaSP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="w3-hover-blue">
            <td>
                @Html.DisplayFor(modelItem => item.MaSP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenSP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GiaSP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaSP"    class="w3-button w3-white w3-border w3-border-red w3-round-large" style=" font-weight: 700;">Sửa</a> 
                <a asp-action="Details" asp-route-id="@item.MaSP" class="w3-button w3-white w3-border w3-border-red w3-round-large" style=" font-weight: 700;">Thông Tin</a> 
                <a asp-action="Delete" asp-route-id="@item.MaSP" class="w3-button w3-white w3-border w3-border-red w3-round-large" style=" font-weight: 700;" >Xóa</a>
            </td>
        </tr>
        
}
    </tbody>
</table>
